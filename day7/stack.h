int push(double *, int, int);      // Adds an element on top of the Stack
int pop(double *, int);            // Removes element from top of the Stack
void top(const double *);          // Returns top element from the Stack else returns -0.01
void displayStack(const double *); // Displays the content of the Stack
int isEmpty(const double *);       // checks if stack is empty